[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "carpet-search"
version = "0.1.0"
description = "Carpet visual search prototype (content-based image retrieval)."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
